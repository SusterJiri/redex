import Config

# Production configuration

# Configure your application to run without console/shell
config :logger, level: :info

# You can add production-specific settings here
# For example, different ports, timeouts, etc.
